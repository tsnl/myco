target
dist
.env

__pycache__

.myco
.myco/worktrees/

# Embed assets live in ~/.myco/models (fetched at runtime), never in the tree.
# Only README + MODEL.manifest are tracked here.
src/text_search/embed_weights/*
!src/text_search/embed_weights/README.md
!src/text_search/embed_weights/MODEL.manifest
*.onnx
*.safetensors
